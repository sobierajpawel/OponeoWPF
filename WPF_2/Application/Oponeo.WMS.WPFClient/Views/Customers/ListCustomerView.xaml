<UserControl x:Class="Oponeo.WMS.WPFClient.Views.Customers.ListCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Oponeo.WMS.WPFClient.Views.Customers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <DataGrid x:Name="employeeGrid" DockPanel.Dock="Left" CanUserAddRows="False"
                  AlternatingRowBackground="LightBlue" AlternationCount="2" AutoGenerateColumns="False"                  
                  ItemsSource="{Binding Customers}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="TaxIdentifier" Binding="{Binding TaxIdentifier}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}"/>
            </DataGrid.Columns>
        </DataGrid>
   

        <ListView DisplayMemberPath="Name" ItemsSource="{Binding Customers}" DockPanel.Dock="Right">
    </ListView>

    </DockPanel>

</UserControl>
